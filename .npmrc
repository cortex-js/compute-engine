loglevel=silent
# npm: don't fail install on stale peerOptional ranges
# (e.g. madge@8 declares peerOptional typescript@^5.4.4 but we run typescript@6)
legacy-peer-deps=true
# pnpm settings
shamefully-hoist=true
strict-peer-dependencies=false